apiVersion: v1
kind: Service
metadata:
  name: posts-srv-multipod
spec:
  type: NodePort
  selector:
    app: posts5
  ports:
    - name: posts5
      protocol: TCP
      port: 4000
      targetPort: 4000
